const Logo = require('./shapes');

class Triangle extends Logo {
    constructor(colour, text, shape) {
        super(colour, text);
        this.shape = triangle;
    }
};

module.exports = Triangle;